<Window x:Class="RX_DigiPrint.Views.UserControls.ShuttingDown"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:common="clr-namespace:RX_Common;assembly=rx_common_lib_cs"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:mvt="clr-namespace:RX_DigiPrint.Helpers"
        Title="{mvt:LocalizedBinding ShuttingDown}"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Height="300" 
        Width="400">

    <Window.Resources>
        <ResourceDictionary>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="120" Margin="5">
            <Image Source="..\..\Resources\Bitmaps\Mouvent_Logo.png" />
            <TextBlock Text="A Bobst Group company  " HorizontalAlignment="Right" FontSize="{StaticResource MvtFontSizeXS}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" >
            <TextBlock Text="{mvt:LocalizedBinding ShuttingDown}"
                     Margin="0,5"
                     HorizontalAlignment="Center" 
                     VerticalAlignment="Center" 
                     Foreground="{StaticResource Mouvent.MainColorBrush}"
                     Background="Transparent"
                     FontSize="20" />
            <ProgressBar x:Name="ProgressBar" Height="20" Width="325" />
            <TextBlock x:Name="Status"
                     Margin="0,5"
                     HorizontalAlignment="Left" 
                     VerticalAlignment="Center" 
                     Foreground="{StaticResource Mouvent.MainColorBrush}"
                     Background="Transparent"
                     FontSize="10" />
        </StackPanel>

        <!-- buttons _______________________________________________________-->
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="1">
            <common:MvtButton Margin="15" Style="{StaticResource Mouvent.RoundIconButtonStyle.LightBackgroundButton.SizeM}" 
                              Click="Cancel_Clicked">
                <iconPacks:PackIconMaterial HorizontalAlignment="Stretch" VerticalAlignment="Center" Width="20" Height="20" 
                                                Kind="Close"></iconPacks:PackIconMaterial>
            </common:MvtButton>
        </StackPanel>
    </Grid>
</Window>
