<UserControl x:Class="rx_rip_gui.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="rx_gi
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContextChanged="UserControl_DataContextChanged"
             >
    
    <Grid x:Name="MainGrid">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <!-- SCREENING ____________________________________________________________________________-->
        <local:RipScreeningView Grid.Row="0" x:Name="ScreeningView"/>

        <!-- RIPPING ______________________________________________________________________-->
        <local:RipRippingView Grid.Row="1" x:Name="RippingView"/>

    </Grid>
</UserControl>
